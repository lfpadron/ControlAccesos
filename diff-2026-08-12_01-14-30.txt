diff --git a/access-manager/frontend/src/views/PisosView.vue b/access-manager/frontend/src/views/PisosView.vue
index d7c15b1..c276a3b 100644
--- a/access-manager/frontend/src/views/PisosView.vue
+++ b/access-manager/frontend/src/views/PisosView.vue
@@ -40,6 +40,7 @@ const torreOptions = computed(() => {
   const campusId = locationContext.campus?.id ?? '';
   return [...torres.value]
     .filter((item) => {
+      if (!item.activo) return false;
       if (campusId) return item.complejo_id === campusId;
       if (!institucionId) return true;
       return campus.value.find((row) => row.id === item.complejo_id)?.institucion_id === institucionId;
